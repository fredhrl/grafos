para utilizar o código deste trabalho:
1)ter python 3 instalado
2)rodar pip installl -r requirements.txt
3)rodar python grafo.py
  a)o programa tem um menu com as opções na mesma ordem do sigaa
  b)além disso, tem as opções criar grafo(onde vc cria um grafo pela cli) e usar um grafo pre definido(que usa um grafo ja contido no sistema)